// Hand-written types for stamp-sw.js, which stays plain JS so build.js and
// watch.js can import it without a TS loader.

export declare function stampServiceWorker(dir?: string, template?: string): string
